DATABASE_URL=sqlite:///./collective.db

# Auth provider
AUTH_PROVIDER=auth0
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_AUDIENCE=https://c0ll3ct1v3-api
AUTH0_ISSUER=https://your-tenant.us.auth0.com/
AUTH0_ALGORITHMS=RS256

# MFA
MFA_REQUIRED=true
MFA_MAX_AGE_SECONDS=900

# Transition controls
ENABLE_LEGACY_PASSWORD_AUTH=false

# CORS (comma-separated; must include every browser origin calling the API)
CORS_ORIGINS=http://localhost:3000,http://localhost:3030,http://127.0.0.1:3000,http://127.0.0.1:3030

# Cloud import — OAuth app credentials (backend only; users sign in with Google/Dropbox)
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
DROPBOX_OAUTH_APP_KEY=
DROPBOX_OAUTH_APP_SECRET=
CLOUD_OAUTH_BACKEND_BASE_URL=http://localhost:8000
CLOUD_OAUTH_FRONTEND_REDIRECT_URL=http://localhost:3030/portal
CLOUD_OAUTH_STATE_SECRET=



# --- Finance integrations (optional) ---
# APP_ENV=development
FINANCE_INTEGRATIONS_ENABLED=true
FINANCE_DEMO_AUTH=false
FINANCE_MFA_CONSUMER_ENFORCED=false
FINANCE_MFA_STEP_UP_FOR_PLAID_LINK=false
FINANCE_MFA_METHODS_ALLOWED=totp,webauthn
FINANCE_MFA_DEMO_OTP=123456
FINANCE_MFA_ADMIN_ENFORCED=true

PLAID_ENV=sandbox
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_WEBHOOK_URL=
PLAID_PRODUCTS=transactions,accounts
PLAID_COUNTRY_CODES=US
PLAID_LINK_MODE=stub

SQUARE_ENV=sandbox
SQUARE_BASE_URL=https://connect.squareupsandbox.com/v2
SQUARE_ACCESS_TOKEN=
SQUARE_WEBHOOK_SIGNATURE_KEY=
SQUARE_WEBHOOK_URL=

ENCRYPTION_KEY_ID=
DATA_RETENTION_RAW_WEBHOOK_DAYS=90
DATA_RETENTION_APP_LOG_DAYS=30
DATA_RETENTION_AUDIT_LOG_DAYS=365

# Local CLI accounting DB (not committed)
# FINANCE_DB_PATH=./var/finances.db

# --- Creative media (DigitalOcean Spaces / MinIO) ---
SPACES_ENABLED=false
SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com
SPACES_PUBLIC_ENDPOINT=
SPACES_REGION=nyc3
SPACES_BUCKET=pj-media
SPACES_ACCESS_KEY=
SPACES_SECRET_KEY=
MEDIA_CDN_BASE_URL=
DEFAULT_MEDIA_TENANT_SLUG=phillipjames
PRIMARY_ARTIST_CLAIM_EMAIL_MARKERS=phillip,phillipjames.com
# PRIMARY_ARTIST_CLAIM_AUTH0_SUBS=auth0|your-sub-here
REDIS_URL=

# --- CLI / automation auth (optional) ---
# Shared secret; scripts send Bearer + X-Tenant-Slug on /manager/*
# C0LL3CT1V3_AGENT_KEY=dev-agent-local

# --- Scoped manager agent (in-process; OpenRouter brain) ---
# OPENROUTER_API_KEY=
# MANAGER_LLM_PROVIDER=openrouter
# MANAGER_LLM_MODEL=anthropic/claude-3.5-haiku
# MANAGER_LLM_MAX_TOKENS=900
# MANAGER_LLM_TIMEOUT_SECONDS=120
# MANAGER_CHAT_RATE_LIMIT_PER_MIN=20
# MANAGER_TOOL_MODE=auto
# auto = Qwen → JSON tool objects (fine-tune friendly); Claude/GPT → native OpenRouter tools

# EPK html_v1 build loop (vision pack → HTML/CSS → Playwright screenshot → vision critique)
# MANAGER_VISION_MODEL=google/gemini-2.0-flash-001
# EPK_PLAYWRIGHT_ENABLED=false
# EPK_SIM_BASE_URL=http://localhost:8080
# EPK_BUILD_MAX_REVISIONS=2

# Optional direct providers (bypass OpenRouter):
# MANAGER_LLM_PROVIDER=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# MANAGER_LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# --- EPK design studio (optional LLM) ---
# EPK_DESIGN_LLM_PROVIDER=openai
# EPK_DESIGN_MODEL=gpt-4o-mini
# Uses OPENAI_API_KEY for generate + Whisper audio brief

# --- Audience mapper (librosa + Spotify Client Credentials) ---
AUDIENCE_ANALYSIS_ENABLED=true
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
# LASTFM_API_KEY=
