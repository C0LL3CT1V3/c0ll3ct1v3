# Sample data

Synthetic CSVs for local development and CI. **Do not** replace these with real exports from banks or payment processors.

Environment variables used by `backend/accounting_core` parsers default to these paths; override as needed.
